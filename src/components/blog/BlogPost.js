import React from 'react';
import PropTypes from 'prop-types';

const BlogPost = (props) => { // eslint-disable-line no-unused-vars
  return (
    <div>
    </div>
  );
}
BlogPost.propTypes = {
  children: PropTypes.arrayOf(PropTypes.element).isRequired
};

export default BlogPost;
